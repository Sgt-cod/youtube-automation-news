name: Gerar e Publicar Vídeo Longo
on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:
jobs:
  gerar-video:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout código
      uses: actions/checkout@v3
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
    
    - name: Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
    
    - name: Instalar FFmpeg e ImageMagick
      run: |
        sudo apt-get update
        sudo apt-get install -y ffmpeg imagemagick
    
    - name: Instalar dependências Python
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Gerar vídeo
      env:
        GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
        YOUTUBE_CREDENTIALS: ${{ secrets.YOUTUBE_CREDENTIALS }}
        VIDEO_TYPE: long
      run: python generate_video.py
    
    - name: Commit logs atualizados
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add videos_gerados.json
        git diff --quiet && git diff --staged --quiet || git commit -m "🎬 Novo vídeo longo gerado - $(date +'%Y-%m-%d')"
        git push
    
    - name: Upload vídeo como artefato
      uses: actions/upload-artifact@v4
      with:
        name: video-long-${{ github.run_number }}
        path: videos/*.mp4
        retention-days: 7
